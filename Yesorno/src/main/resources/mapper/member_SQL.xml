<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.yesorno.dao.MemberDao">
<!-- 
	<select id="listDao" resultType="com.javalec.springEx.dto.BoardDto">
		SELECT * FROM MINI_BOARD ORDER BY MID DESC
	</select>
-->	
	<insert id="memberRegistration">
 		INSERT INTO MEMBERS_TB(MEMBER_ID,MEMBER_EMAIL,MEMBER_NICKNAME,MEMBER_JOIN_DATE,MEMBER_TOKEN,MEMBER_GRADE_ID) 
		VALUES(0,#{memberEmail},#{memberNickname},#{memberJoinDate},#{memberToken},#{memberGradeId});
	</insert>
<!--
	<delete id="deleteDao">
		DELETE FROM MINI_BOARD WHERE mId=#{param1}
	</delete>
	<select id="viewDao" resultType="com.javalec.springEx.dto.BoardDto">
		SELECT * FROM MINI_BOARD WHERE mId=#{param1}
	</select>
-->
</mapper>